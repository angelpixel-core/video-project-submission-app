# Containers

This directory contains container build contexts and runtime support files.

## Layout

- `app/` - Application container definition and entrypoint.
- `mysql/` - Database bootstrap scripts.

## Notes

- Each container subdirectory should document its own purpose with a README.

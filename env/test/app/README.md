# Test App Variables

This directory contains test app runtime values.

## Files

- `core.env` - Non-secret CI runtime values.
- `db.env` - Non-secret CI database connection values. Use `secrets.local.env` for placeholder secret values when needed.

## Notes

- These files are versioned because they support CI.

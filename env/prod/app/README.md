# Production App Variables

This directory contains production app runtime values.

## Files

- `core.env` - Non-secret runtime values.
- `db.env` - Local-only database connection values.

## Notes

- `db.env` and `secrets.local.env` can hold placeholder secret values in production.

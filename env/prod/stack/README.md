# Production Stack Variables

This directory contains production stack orchestration values.

## Files

- `compose.env` - Non-secret stack configuration.

## Notes

- `compose.env` should define `STACK_ENV=prod`.

# Staging Database Variables

This directory contains staging database bootstrap values.

## Files

- `bootstrap.env` - Bootstrap values; use `secrets.local.env` for placeholder secret overrides when needed.

## Notes

- Keep bootstrap secrets out of git and sync them separately.

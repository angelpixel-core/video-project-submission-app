# Staging App Variables

This directory contains staging app runtime values.

## Files

- `core.env` - Non-secret runtime values.
- `db.env` - Local-only database connection values.

## Notes

- `db.env` should be treated as a local secret file in staging.

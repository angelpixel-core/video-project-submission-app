<div class="col-12 col-lg-6" data-project-id="<%= project.id %>">
  <div class="card h-100 shadow-sm border-0 client-project-card">
    <div class="card-body">
      <div class="d-flex justify-content-between align-items-start gap-3 mb-3">
        <% details_path = project.draft? ? edit_project_path(project) : project_path(project) %>
        <% show_raw_footage_url = !project.raw_footage_previewable? %>
        <div>
          <h2 class="h5 mb-1">
            <%= link_to details_path, class: "client-project-title-link text-reset text-decoration-none", data: { role_scope: "client" } do %>
              <%= project.name.presence || "Untitled project" %>
            <% end %>
          </h2>
          <% if show_raw_footage_url %>
            <p class="mb-0 text-body-secondary"><%= project.raw_footage_url.presence || "No footage URL yet" %></p>
          <% end %>
        </div>
        <div class="d-flex flex-column align-items-end gap-2">
          <div id="<%= project.status_badge_dom_id %>">
            <%= render "status_badge", project: project %>
          </div>
          <% if project.draft? %>
            <%= link_to "Reanudar", edit_project_path(project), class: "btn btn-sm btn-outline-primary", data: { role_scope: "client" } %>
          <% end %>
        </div>
      </div>

      <%= render "raw_footage_preview", project: project, parent_host: request.host, preview_mode: :card if project.raw_footage_previewable? %>

      <div class="mb-3">
        <p class="text-uppercase small text-body-secondary mb-2">Video types</p>
        <% if project.video_types.any? %>
          <ul class="list-unstyled mb-0">
            <% project.video_type_selections.includes(:video_type).each do |selection| %>
              <li class="d-flex justify-content-between gap-3 py-1">
                <span><%= selection.video_type.name %></span>
                <span class="text-body-secondary">Qty <%= selection.quantity %></span>
              </li>
            <% end %>
          </ul>
        <% else %>
          <p class="mb-0 text-body-secondary">No selections recorded yet.</p>
        <% end %>
      </div>

      <div class="small text-body-secondary">
        PM: <%= project.pm.name %>
      </div>
    </div>
  </div>
</div>

# Payments Webhooks

Current files:

- `payment_webhooks_controller.rb`
- `event/`

## Rule

- Webhook code receives provider payloads, validates them, and hands them to payment application handlers.

# Payments Outbound Adapters

Current outbound subtree:

- `gateways/`
- `mailers/`
- `webhooks/`
- `persistence/`

## Rule

- Outbound adapters are called by payment application code to reach external systems or local storage edges.

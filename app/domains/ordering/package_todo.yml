# This file contains a list of dependencies that are not part of the long term plan for the
# 'app/domains/ordering' package.
# We should generally work to reduce this list over time.
#
# You can regenerate this file using the following command:
#
# bin/packwerk update-todo
---
".":
  "::ApplicationRecord":
    violations:
    - dependency
    files:
    - app/domains/ordering/adapters/persistence/order/order_line_record.rb
    - app/domains/ordering/adapters/persistence/order/order_record.rb
    - app/domains/ordering/adapters/persistence/order/source_video_record.rb
  "::NotificationJob":
    violations:
    - dependency
    files:
    - app/domains/ordering/application/commands/process_submission.rb
  "::VideoType":
    violations:
    - dependency
    files:
    - app/domains/ordering/adapters/persistence/order/order_line_record.rb

# Billing Stream Presentation

Use this folder for realtime or streamed billing output.

The directory stays under `billing/` physically while the product vocabulary moves to `invoicing`.

Examples:

- Turbo Streams
- SSE payloads
- websocket payload builders

## Rule

- Stream presentation is a transport shape, not a business rule.

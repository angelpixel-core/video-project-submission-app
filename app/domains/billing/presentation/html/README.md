# Billing HTML Presentation

Use this folder for HTML-shaped billing output.

Examples:

- ERB templates
- ViewComponent renderers
- HTML document presenters

## Rule

- HTML presentation may use Rails helpers at the edge, but billing data shaping should stay explicit.

# Billing Entities

Billing domain entities belong here.

The folder remains physically named `billing/` even as the product vocabulary shifts to `invoicing`.

Current entities:

- `credit_note.rb`
- `invoice_line.rb`

## Rule

- Entities model billing concepts that are not aggregate roots.

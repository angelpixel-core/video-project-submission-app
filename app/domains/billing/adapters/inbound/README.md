# Billing Inbound Adapters

Inbound adapters receive external input and hand it to billing use cases.

## Future / Allowed Shapes

- `event_consumers/`
- `http/`
- `graphql/`
- `grpc/`
- `websocket/`
- `iot/`

## Rule

- Inbound adapters translate external protocol details into application calls.

# This file contains a list of dependencies that are not part of the long term plan for the
# 'app/domains/capacity' package.
# We should generally work to reduce this list over time.
#
# You can regenerate this file using the following command:
#
# bin/packwerk update-todo
---
".":
  "::Core::Services::Provider":
    violations:
    - dependency
    files:
    - app/domains/capacity/application/ports/clock.rb
    - app/domains/capacity/application/ports/workload_source.rb
  "::Project":
    violations:
    - dependency
    files:
    - app/domains/capacity/domain/policies/capacity_calculation_policy.rb
